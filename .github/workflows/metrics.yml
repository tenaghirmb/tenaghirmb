name: Metrics
on:
  schedule: [{cron: "0 */12 * * *"}]
  workflow_dispatch:
  push: {branches: ["master", "main"]}

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@master
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          user: tenaghirmb
          template: classic
          base: header
          base_indepth: yes
          config_timezone: Asia/Shanghai
          plugins_errors_fatal: no

          plugin_music: no
          plugin_music_provider: spotify
          plugin_music_mode: playing
          plugin_music_token: "${{ secrets.SPOTIFY_TOKENS }}"
          plugin_music_limit: 4
          plugin_music_played_at: yes

          plugin_languages: yes
          plugin_languages_details: bytes-size, percentage
          plugin_languages_ignored: html, css, tex
          plugin_languages_colors: rainbow
          plugin_languages_sections: recently-used
          plugin_languages_recent_load: 10
          plugin_languages_recent_days: 1
          plugin_languages_limit: 8
          plugin_languages_skipped: github-actions[bot]

          plugin_habits: no
          plugin_habits_from: 200
          plugin_habits_days: 14
          plugin_habits_facts: yes
          plugin_habits_charts: yes
          plugin_habits_charts_type: classic

          plugin_topics: yes
          plugin_topics_limit: 12
          plugin_topics_mode: icon

          plugin_isocalendar: yes
          plugin_isocalendar_duration: half-year

          plugin_wakatime: yes
          plugin_wakatime_token: ${{ secrets.WAKATIME_API_KEY }}
          plugin_wakatime_days: 7
          plugin_wakatime_sections: time, projects, languages, editors
          plugin_wakatime_limit: 5
          plugin_wakatime_user: current

          config_order: base.header, isocalendar, languages, topics, wakatime, music, habits
